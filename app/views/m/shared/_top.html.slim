.ctor-info
  = link_to t_t('name'), m(site_url)
  | &nbsp;
  = link_to t('_forum'), m(sub_site('bbs'))
  | &nbsp;
  - if session[:id].nil?
    = link_to t('_login'), m(site_url+login_path)
    | &nbsp;
    = link_to t('_register'), m(site_url+register_path)
  - else    
    = link_to t('_active'), m(site_url+like_path)
    | &nbsp;
    = link_to session[:name], m(site_url)
    br
    = link_to t('write_note'), m(site_url+new_note_path)
    | &nbsp;
    = link_to t('p_blog'), m(site_url+new_blog_path)
    | &nbsp;
    = link_to t('upload_photo'), m(site_url+upload_photo_path)