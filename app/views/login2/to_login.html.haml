%h1 管理员登录
%p#notice
  =notice
=form_tag "login2/login" do
  .field
    =label_tag :passwd,"密码"
    %br
    =password_field_tag :passwd
  .actions
    =submit_tag("登录")
  
  提示：
  -s='http://'+@user.username+'.diancai.la'
  =link_to s,s
  是本站的永久访问地址