.b
  - if request.subdomain.to_s==""
    | 标题
  - else
    | 标题或正文
  | ：
  = text_field_tag :title, params[:title]
  | &nbsp;
  input type='button' value=t'_search' onclick='goSearch()'
coffee:
  this.goSearch = ->
    window.location.href = "/s/" + $('#title').val()